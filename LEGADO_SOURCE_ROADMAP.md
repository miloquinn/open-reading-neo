# 更多书源协议实施路线

更新日期：2026-07-31

## 目标与边界

Open Reading 将通过默认关闭的“更多书源协议”高级功能，支持导入和使用
Legado 风格书源。该能力是应用内的兼容扩展，不属于 ORSP 规范，也不代表对所有
Legado 规则、站点可用性或内容授权的保证。

高级功能关闭时：

- 不显示 Legado 导入入口；
- 不加载或执行已导入的 Legado 书源；
- 现有 ORSP 书源和本地阅读行为保持不变。

其他协议书源必须在导入时完整跑通“搜索 -> 详情 -> 目录 -> 首章正文”才会
保存并自动启用；检测失败的条目不进入已接入列表。导入配置不得自动执行登录、
JavaScript、WebView、付费或其他有外部副作用的动作。

## 第一阶段：导入与声明式核心运行时

状态：**部分完成（文本声明式子集可用）**

目标是让一批不依赖登录、JavaScript 或 WebView 的真实书源完成“导入 -> 搜索 ->
详情 -> 目录 -> 正文 -> 阅读/下载”链路。

### 导入与管理

- [x] 设置页新增“高级功能”区域和默认关闭的“更多书源协议”开关；
- [x] 支持本地 JSON 文件导入；
- [x] 支持 URL 导入；
- [x] 支持单对象、对象数组和 `{ "sourceUrls": [...] }` 聚合入口；
- [x] 设置文件大小、下载大小、重定向、嵌套 URL 数量和条目数量限制；
- [x] 按 `bookSourceUrl` 去重，同 URL 更新时不覆盖用户启用状态；
- [x] 导入前显示“可使用 / 部分支持 / 暂不支持”及具体原因；
- [x] 聚合文件只检测有上限的候选条目，仅保存并自动启用完整读取链通过的来源；
- [x] 支持文本、图片/漫画和文件类型的识别；音频、视频明确拒绝。

### 请求与规则子集

- [x] GET 和受限 POST 请求；HEAD 留到后续下载阶段；
- [x] URL 模板变量 `{{key}}`、`{{page}}` 和相对 URL；
- [x] UTF-8、GBK/GB2312/GB18030 等常见字符集；
- [x] 静态请求头和表单请求体；
- [x] 默认 DOM/JSoup 风格选择规则及 CSS 选择器；
- [x] 基础 JSON 对象路径和数组索引；
- [ ] XPath 与复杂 JSONPath 留在后续兼容层；
- [x] `&&` 拼接、`||` 回退和 `##` 正则清洗/替换；
- [x] 搜索、详情、目录、分页正文的确定性标识；
- [x] 声明式发现入口：旧式 `标题::URL`、JSON `type=url`、`{{page}}` 分页、
  `ruleExplore` 书单规则及缺失时回退 `ruleSearch`；
- [x] 网络请求复用 ORSP 的 SSRF、DNS、重定向和响应大小边界。

### 第一阶段不执行

- JavaScript、`mainJs`、`jsLib`、`@js:`、`<js>`；
- WebView、`webJs`、后台浏览器和页面事件；
- 登录 UI、登录检测、账号凭据和验证码；
- CookieJar、跨请求会话和动态 Token；
- 自定义 DNS、代理、文件系统访问和任意上传；
- 封面/图片解密脚本、购买操作、自定义按钮和事件回调。

检测到上述能力的书源仍可出现在预检结果中，但必须标为“暂不支持”，不得降级后
静默执行。

## 第二阶段：图片、文件与受控登录

状态：**未开始**

- 完整漫画图片章节与离线下载；
- 文件型书源的下载链接、文件格式识别、大小限制和导入交接；
- 书源自定义登录表单；
- 验证码图片和显式用户提交；
- 登录状态检测；
- Cookie/Token 按书源与域名隔离；
- 凭据存入系统安全存储，不进入书源 JSON、日志、书架快照或 WebDAV；
- 删除书源时可同时清除凭据和会话。

第二阶段不包含自动购买、自动签到或其他未由用户逐次确认的外部操作。

## 第三阶段：受限脚本与动态页面

状态：**未开始，需独立安全评审**

- JavaScript 规则的隔离运行时、CPU/内存/时间预算和 API 白名单；
- `jsLib` 的来源完整性、缓存和版本策略；
- WebView 动态页面规则和生命周期隔离；
- 图片解密、事件回调和自定义按钮；
- 恶意规则、跨源数据读取、无限循环和资源耗尽测试；
- iOS、Android、桌面平台的发行政策与能力差异评审。

完整 Legado 脚本兼容不作为默认承诺。无法满足隔离要求的平台应继续只运行声明式子集。

## 真实样本基线

开发样本位于用户本机，不提交到仓库：

- `bookSource-230824.json`：4513 条；
- `shareBookSource.json`：3949 条；
- `shuyuan.json`：1061 条。

合计 9523 条记录、7497 个唯一 `bookSourceUrl`，其中 2026 条为重复 URL。样本包含
JavaScript、WebView、登录、Cookie、XPath、JSONPath、图片和文件型书源，不能直接全量
启用。测试应从样本中抽取去标识化的小型夹具，覆盖可支持、部分支持和拒绝三类，不把
聚合文件或第三方内容提交到项目。

## 完成标准

每个阶段只有同时满足以下条件才能标记完成：

1. 功能开关、导入、删除、启停和升级路径有回归测试；
2. 可支持样本完成真实或本地夹具的端到端阅读链路；
3. 不支持规则在发起网络请求前被明确拒绝；
4. 凭据、Cookie、脚本和私网访问没有越过阶段边界；
5. 静态分析、相关测试和 `git diff --check` 通过；
6. 本文档同步更新状态、验证证据和已知限制。

## 进度记录

### 2026-08-02

- 重新对照 Legado-E 导入链路，撤销“导入必须通过搜索、详情、目录和首章正文实测”的策略；
  导入现在只做本地结构解析、URL 去重、静态兼容标记和单次批量保存，联网校验不再是保存门槛；
- 所有合法配置进入管理列表：当前声明式运行时可执行的来源按原配置启用，其余停用并标记
  “待兼容”。`BookSourceRegistry.load()` 不再隐藏没有在线验证时间戳的 Legado 记录，
  `loadRunnable()` 继续执行高级协议总开关和运行能力边界；
- 22 MiB 聚合文件在后台 isolate 单次解码，URL 导入不再重复下载；注册表批量保存后直接返回
  内存结果，避免再解码整份 20+ MiB JSON；
- `enabledCookieJar` 和可选登录配置不再被误当成核心阅读依赖；显式 Cookie 请求头、脚本、
  WebView、XPath、复杂 JSONPath 与非文字源仍在请求前拒绝；
- 管理页增加搜索、启停/兼容状态筛选、可搜索分组筛选、当前结果批量操作和 Sliver 惰性列表；
- 真实回归：`shuyuan.json` 导入 1,048/1,061 条，`shareBookSource.json` 导入
  3,880/3,949 条；跳过项均为缺少名称/URL 或非 HTTP(S) 占位配置。后台解析约 0.27/0.60 秒，
  第二份文件转换序列化约 0.62 秒、批量保存约 0.28 秒，全程没有访问任何书源站点。

### 2026-08-01

- 参考 Legado-E 提交 `8b87c5a` 梳理“书源 -> 发现频道 -> 分页书单”链路；
- 新增独立声明式发现解析器，只接受旧式 `标题::URL` 与 JSON `type=url`，入口 URL
  在展示前通过现有受限请求模板预检，任意外部传入的未声明频道 URL 在请求前拒绝；
- `LegadoCompatibilityScanner` 将核心阅读配置与可选发现配置分开扫描：发现使用脚本或
  高级控件时只关闭该源的发现能力，不再误伤已验证的搜索、详情、目录和正文；
- 已验证的历史存储书源在读取时自动恢复安全的 `categories` / `browse` 能力，不要求重导；
- 发现页统一为“先选书源、再选栏目/频道、下方看书”的操作路径；Legado 频道直接进入
  统一书单，支持去重分页、失败重试、详情、阅读和加入书架，ORSP 推荐/分类/最新保持不变；
- 动态 `@js:` / `<js>` 发现、`text/button/toggle/select`、`infoMap` 与动作回调继续明确
  留在受限脚本阶段，本阶段不会静默执行；
- 验证：Legado 解析/运行时/安全边界 33 项、完整发现页 14 项、统一搜索/发现流程 7 项、
  导入分析 3 项通过；触达文件静态分析无问题，全仓静态分析无 error/warning，保留
  60 条既有 info 级提示。

### 2026-07-31

- 完成 Legado 上游、延续 fork、第三方解析器和三份真实聚合样本的格式研究；
- 确认 Legado 是随应用演进的 JSON 模型和规则运行时，不是固定版本化网络协议；
- 确定高级功能默认关闭、导入默认停用、首期只运行声明式子集；
- 完成默认关闭开关、JSON/URL 导入、批量去重、兼容性预检和管理入口；
- 添加书源统一为“导入链接 / 从 JSON 文件添加”两种方式，自动识别 ORSP 与其他协议；
- 已接入书源按“ORSP 书源 / 其他协议书源”分组，聚合 JSON 上限提升至 64 MiB；
- 其他协议书源导入时执行“搜索、详情、目录、首章正文”实际检测，任一阶段失败都不保存；
  通过完整检测的书源自动启用；
- 管理页支持全选、批量启用、批量停用和批量删除；添加面板不再自动弹出键盘；
- 完成受限 GET/POST、常见字符集、老式 DOM/CSS、基础 JSON 路径和四段文本阅读链路；
- 通过本地 HTML/JSON 夹具端到端测试；真实聚合样本只用于静态筛选；
- 静态规则扫描只用于缩小检测范围，不再显示或保存为“可使用”；导入时最多检测 120 个
  候选，最多保留 30 个实际搜索返回有效结果的来源，并自动启用；
- 已知限制：图片/文件只识别不执行，XPath、复杂 JSONPath、脚本、登录、Cookie 和
  WebView 仍在发起网络请求前拒绝。
- Legado 响应的 GBK/GB2312/GB18030 兼容解码改用 APP 内已验证的快速解码路径，
  避免真实大型聚合样本检测时第三方字符映射懒加载导致 Dart VM 崩溃。
- 修正备注文本被误判为 XPath、`+@css:` / `@css:` 声明前缀和两段式正则列表捕获；
  `shareBookSource.json` 的 120 个有界候选检测实际通过 2 个来源，其中
  `http://www.botaodz.com` 验证时搜索返回 20 条、详情正确、目录 1914 章、
  首章正文 3011 字符；另一通过来源为 `http://www.booksky.cc/`。
- 验证：兼容运行时与相关书源回归测试 85 项通过，新增核心测试 24 项通过；相关新增/修改文件静态分析无问题；
  全仓静态分析没有 error，但仍有 58 条既存 info 级提示。
