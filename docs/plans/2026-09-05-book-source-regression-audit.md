# 书源详情与 4020 规则回归复查

日期：2026-09-05。范围：详情元数据合并、导入规则执行、目录与正文缓存。

## 已复现并修正

| 问题 | 修正位置 | 回归证据 |
| --- | --- | --- |
| 详情返回空作者或空简介，覆盖列表已有内容 | `sourced_book_details_controller.dart` | controller 与 sheet 测试覆盖空白、空 HTML、有效更新和请求失败 |
| `###` 被当作普通替换，生成嵌套的错误书籍 URL | `source_rule_parser.dart`、`source_rule_engine.dart` | 原始 4020 URL 规则，同步与异步结果一致；普通替换保留全局替换语义 |
| `@js:` 后的 `##` 正则被一起传入 JS，目录标题解析失败 | `source_rule_parser.dart`、`source_rule_script.dart` | 字符串、注释、正则字面量、模板、除法、控制语句和空脚本边界 |
| 无选择器的后处理重新解析文本，插入 HTML 包装 | `source_rule_engine.dart` | 对原始文本做全局替换和 OnlyOne 提取，保持预期文本 |
| `java.getStringList` 丢失每一项的 OnlyOne 语义 | `source_script_dom_api.dart` | 真实 QuickJS 同步和异步多链接提取 |
| 引擎修正后仍可能命中旧目录和正文磁盘缓存 | `reading_source_backend.dart` | 引擎缓存版本升至 2；预置版本 1 文件，确认重新加载并持久化新结果 |
| 登录脚本可能先改变会话或 Cookie 再抛错，旧身份内容仍被复用 | `source_runtime.dart`、`source_runtime_state.dart`、`reading_source_backend.dart` | 认证入口在 finally 定向失效；失败登录保留原始错误，新 backend 不再命中旧身份目录和正文磁盘缓存 |

会话保存和清除在底层存储抛错后也各有动态回归：精确断言原始错误，随后
读取目录时确实重新请求详情；变更一个书源不会清除其他书源状态。

`test/fixtures/book_sources/4020_bybk.json` 的五组规则、请求头和源地址已逐项
与用户提供的 `shuyuan.json` 原记录比较，内容相同。完整运行时用例使用原创
模拟 HTML，验证发现页 → 详情 → 目录 → 两章正文，以及最终请求 URL、
重定向后的相对地址解析和正文清理。测试不依赖用户本地书源目录或网络。

## 防止回归

新增测试自动进入现有 PR checks 的 `*_test.dart` 扫描，无需另建手动流程。
规则兼容性变更需要保留实际失败规则，并验证后续阅读步骤、同步与异步入口，
以及旧磁盘缓存的失效。具体约定见 `test/fixtures/book_sources/README.md`。

独立代码审查确认规则语义分支无未修复问题；架构复查推动修正了引擎版本与
认证失败路径的缓存失效。删除了旧的“空选择器意味着 OnlyOne”推断，以显式
规则标记统一处理；没有新增依赖。

## 最终验证

- 23 个相关测试文件，196 项测试全部通过。18 个文件逐文件隔离执行，
  另补 XPath/HTML 18 项及认证失效 8 项；重复验证不重复计数。
- 全项目 `flutter analyze --no-pub` 无问题；最后追加认证测试后，相关源码
  和测试再次 `dart analyze` 无问题。
- 修改文件格式检查、`git diff --check` 通过。
- 代码审查通过；架构审查当前阻塞为 0，以下两个关注项仍保留。
- 未打包、发布或安装手机版本；本记录仅声明当前工作区代码的验证结果。

## 验证边界与后续关注

- 本轮读取了 1061 条书源配置；没有逐源验证网络可用性。
- 4020 旧站地址曾返回跳转，新站直连测试未正常返回页面。离线规则回归通过
  不代表实站 403、反爬策略或手机网络问题已经解决；尚未安装到手机验证。
- 详情响应复用缓存按条目数限制，尚无总字节预算或过期时间。架构复查将其
  列为内存与时效性关注项，本轮没有扩大到整个缓存策略重写。
- UI 的空作者回退与规则上下文中的作者回退仍是两个层次。当前书源样本未
  找到同时依赖该边界的失败实例；依赖 `book.author` 的后续规则需要独立用例。
